#ifndef EDITMODE_H
#define EDITMODE_H
#endif // EDITMODE_H
#include "xtc.h"

std::vector<std::vector<mdata>> editmode(std::vector<std::vector<mdata>>& matrixData, Ui::XTC *ui);

void dataPlot_E(std::vector<std::vector<mdata>>& matrixData, Ui::XTC *ui);

void coreItemChecked_E(std::vector<std::vector<mdata>>& matrixData, Ui::XTC *ui);
